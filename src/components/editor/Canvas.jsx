const Canvas = ({ width, height }) => {
  return (
    <div
      className="w-full h-full bg-white rounded-[20px]"
      style={{ width, height }}
    >
      Canvas elements drop here
    </div>
  );
};

export default Canvas;
